/**
 * @author Bruna Vieites - fc55792
 */

import { SimplePatient } from './simple-patient.model';

describe('SimplePatient', () => {
  it('should create an instance', () => {
    expect(new SimplePatient()).toBeTruthy();
  });
});
