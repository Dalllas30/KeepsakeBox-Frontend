/**
 * @author André Santana - fc49451
 */

import { PersonalImageList } from './personal-image-list.model';

describe('PersonalImageList', () => {
  it('should create an instance', () => {
    expect(new PersonalImageList()).toBeTruthy();
  });
});
