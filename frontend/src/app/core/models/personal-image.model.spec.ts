/**
 * @author André Santana - fc49451
 */

import { PersonalImage } from './personal-image.model';

describe('PersonalImage', () => {
  it('should create an instance', () => {
    expect(new PersonalImage()).toBeTruthy();
  });
});
