/**
 * @author André Santana - fc49451
 */

import { AddImageData } from './add-image-data.model';

describe('AddImageData', () => {
  it('should create an instance', () => {
    expect(new AddImageData()).toBeTruthy();
  });
});
