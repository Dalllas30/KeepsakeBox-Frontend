/**
 * @author André Santana - fc49451
 */

import { PatientTag } from './patient-tag.model';

describe('PatientTag', () => {
  it('should create an instance', () => {
    expect(new PatientTag()).toBeTruthy();
  });
});
