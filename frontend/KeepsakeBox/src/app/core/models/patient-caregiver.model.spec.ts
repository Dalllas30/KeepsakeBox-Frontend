/**
 * @author André Santana - fc49451
 */

import { PatientCaregiver } from './patient-caregiver.model';

describe('PatientCaregiver', () => {
  it('should create an instance', () => {
    expect(new PatientCaregiver()).toBeTruthy();
  });
});
