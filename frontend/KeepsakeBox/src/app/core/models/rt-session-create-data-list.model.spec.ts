import { RtSessionCreateDataList } from './rt-session-create-data-list.model';

describe('RtSessionCreateDataList', () => {
  it('should create an instance', () => {
    expect(new RtSessionCreateDataList()).toBeTruthy();
  });
});
