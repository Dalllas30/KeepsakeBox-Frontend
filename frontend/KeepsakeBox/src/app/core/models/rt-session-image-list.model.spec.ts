import { RtSessionImageList } from './rt-session-image-list.model';

describe('RtSessionImageList', () => {
  it('should create an instance', () => {
    expect(new RtSessionImageList()).toBeTruthy();
  });
});
