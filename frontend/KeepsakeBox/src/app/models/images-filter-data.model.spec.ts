import { ImagesFilterData } from './images-filter-data.model';

describe('ImagesFilterData', () => {
  it('should create an instance', () => {
    expect(new ImagesFilterData()).toBeTruthy();
  });
});
