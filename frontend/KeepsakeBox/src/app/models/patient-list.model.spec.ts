/**
 * @author André Santana - fc49451
 */

import { PatientList } from './patient-list.model';

describe('PatientList', () => {
  it('should create an instance', () => {
    expect(new PatientList()).toBeTruthy();
  });
});
