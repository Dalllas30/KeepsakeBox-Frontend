import { TemplateSessionData } from './template-session-data.model';

describe('TemplateSessionData', () => {
  it('should create an instance', () => {
    expect(new TemplateSessionData()).toBeTruthy();
  });
});
