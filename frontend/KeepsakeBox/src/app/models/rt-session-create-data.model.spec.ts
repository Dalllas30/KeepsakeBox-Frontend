import { RtSessionCreateData } from './rt-session-create-data.model';

describe('RtSessionCreateData', () => {
  it('should create an instance', () => {
    expect(new RtSessionCreateData()).toBeTruthy();
  });
});
