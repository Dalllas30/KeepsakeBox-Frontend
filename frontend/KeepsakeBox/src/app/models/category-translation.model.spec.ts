import { CategoryTranslation } from './category-translation.model';

describe('CategoryTranslation', () => {
  it('should create an instance', () => {
    expect(new CategoryTranslation()).toBeTruthy();
  });
});
