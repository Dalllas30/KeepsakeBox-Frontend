import { TemplateSessionList } from './template-session-list.model';

describe('TemplateSessionList', () => {
  it('should create an instance', () => {
    expect(new TemplateSessionList()).toBeTruthy();
  });
});
